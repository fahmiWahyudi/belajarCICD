apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: training-cicd-ingress
  namespace: fahmiwah # change if your namespace is different
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: fahmiwah.57.158.184.247.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: training-cicd-svc
                port:
                  number: 80
